apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    # Grafana reaches Prometheus over the compose network on its container port.
    url: http://prometheus:9090
    isDefault: true
